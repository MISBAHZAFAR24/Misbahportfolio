// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // Hum yahan se plugins hata rahe hain kyunki ye Flutter ke 'android' folder se conflict kar rahe hain.
}